---
title: Add
id: add_instruction
---

# Add

The add instruction add two numbers.

## Example

```
start:
    move eax <= 1
    move ebx <= 2
    add eax <= ebx
```

## Equivalent in assembly

The equivalent in assembly is ```add```